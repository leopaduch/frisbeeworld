name: Update DGPT standings

on:
  schedule:
    - cron: '0 14 * * 1'   # Every Monday at 9am ET (14:00 UTC)
  workflow_dispatch:         # Also lets you trigger it manually from GitHub

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install
        working-directory: scripts

      - name: Fetch standings
        run: node fetch-standings.js
        working-directory: scripts

      - name: Commit updated data
        run: |
          git config user.name  "DGPulse Bot"
          git config user.email "bot@dgpulse"
          git add data/standings.json
          git diff --staged --quiet || git commit -m "standings: auto-update $(date +'%Y-%m-%d')"
          git push
